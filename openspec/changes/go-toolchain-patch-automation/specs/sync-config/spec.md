## REMOVED

This capability is no longer part of the change. The Renovate-based
approach does not modify `sync-config.yml`. Target repositories are
defined in `renovate-config.js` in org-infra. See
`specs/renovate-runner/spec.md` for the replacement.
